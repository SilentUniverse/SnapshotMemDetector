{
  "name": "heap-analyzer",
  "version": "1.0.0",
  "private": true,
  "description": "Static V8 heap snapshot/timeline analyzer built on memlab. Reports raw growing objects, retainer paths, dominator paths, and timeline stack candidates without business translation rules.",
  "type": "commonjs",
  "engines": { "node": ">=18" },
  "scripts": {
    "analyze": "node --max-old-space-size=12288 analyze.js"
  },
  "dependencies": {
    "@memlab/api": "^1.0.0"
  }
}
